<form method="post" action="{{ path('app_edit_dictionary_delete', {'id': dictionary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dictionary.id) }}">
    <button class="btn">Delete</button>
</form>
